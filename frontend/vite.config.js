// Diese Datei wurde automatisch generiert und dient dazu, Vite zu konfigurieren. 
// Vite ist das, was es ermöglicht, React in HTML, CSS, JSS zu kompilieren.


import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
})
